$kind: http-request
description: Login with username or email and password - use returned token for
  authenticated requests
url: "{{base_url}}/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "username": "john_doe",
      "email": null,
      "password": "SecurePassword123"
    }
order: 2000
